2) One would need to make sure that there were three tables, each with a primary key, and three foreign keys in the join table that referred to each of those primary keys.


3) Instead of just typing in 'SELECT *', one can instead type in all the fields of data after 'SELECT'

4) One could do an outer join for all three tables that the business is connected to. Each business should have all their data listed.