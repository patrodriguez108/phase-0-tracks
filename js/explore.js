// declare javascript function with parameter

// write out argument that would reverse the parameter
  // split word into array
  // reverse array
  // join array

// test out function with driver code

function reverse(word){
  return word.split("").reverse().join("");
}

var jellyReverse = console.log(reverse("jelly"));

if (1==1) {
  jellyReverse
}